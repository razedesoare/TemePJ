spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lab10
    username: root
    password: parolasql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
